/**
 * Export centralisé des hooks personnalisés
 */

export { usePrayerTimes } from './usePrayerTimes';
export type {
  Prayer,
  PrayerName,
  CountdownData,
  UsePrayerTimesResult,
} from './usePrayerTimes';

// Placeholder pour les futurs hooks
// export { useBookmarks } from './useBookmarks';
// export { useLocation } from './useLocation';
